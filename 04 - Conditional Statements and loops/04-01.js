/*Write a JavaScript program that accept two integers and display the larger.*/

function largeNum (a,b) {
    if (a > b) return console.log(`${a} is larger.`)
    else return console.log(`${b} is larger.`)
}

largeNum(2,3);
largeNum(40,39);
largeNum(10002,12202);